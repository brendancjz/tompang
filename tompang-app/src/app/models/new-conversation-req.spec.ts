import { NewConversationReq } from './new-conversation-req';

describe('NewConversationReq', () => {
  it('should create an instance', () => {
    expect(new NewConversationReq()).toBeTruthy();
  });
});
